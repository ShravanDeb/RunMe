# Firebase (required for API routes)
FIREBASE_SERVICE_ACCOUNT_PATH=./service-account.json
# OR use project ID only (for emulators)
FIREBASE_PROJECT_ID=runme-portfolio

# JWT secret for auth tokens
JWT_SECRET=your-secret-key-here

# GitHub token (optional, for fetching stats without rate limits)
GITHUB_TOKEN=ghp_xxxxxxxxxxxx
